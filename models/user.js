export default class User {
    constructor(username, email,cridt,pass,repass) {
      this.username = username;
      this.email = email;
      this.cridt = this.cridt;
      this.pass = pass;
      this.repass = repass;
    }
  }